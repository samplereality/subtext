{
  "name": "trialogue",
  "version": "2.0.0",
  "description": "A chat-style story format for Twine 2",
  "author": "Philo van Kemenade",
  "license": "MIT",
  "repository": "https://github.com/samplereality/trilogue-relog",
  "config": {
    "formatName": "Trialogue",
    "distDir": "dist/"
  },
  "scripts": {
    "build": "node build.js",
    "demo": "node build.js && node scripts/build-demo.js",
    "test": "node build.js && node scripts/build-demo.js && node scripts/smoke-test.js"
  },
  "dependencies": {
    "lz-string": "^1.5.0",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "playwright": "^1.49.1"
  }
}
